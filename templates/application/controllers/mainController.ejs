import { <%= nameFunction %>UseCase } from '@domain/useCase/<%= nameFunction %>UseCase';

export const <%= nameFunction %>Controller= async(event) =>{
    return await <%= nameFunction %>UseCase(event);
}