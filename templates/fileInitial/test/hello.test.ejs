import {getHello} from '../src/infraestructure/handlers/hello-handler'

describe('Test de prueba is genered',()=>{
    test("segundo test",async ()=> {
        const result = await getHello("string event");
        expect(result.statusCode).toBe(200);
    })
})